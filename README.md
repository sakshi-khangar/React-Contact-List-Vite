# React-Contact-List-Vite
 This is a simple React Contact List application built using Vite.  It displays multiple contact cards with name, email, phone number,  and address. The project demonstrates the use of React components,  props, CSS styling, and rendering lists using map().  Vite is used for fast development and build performance.
